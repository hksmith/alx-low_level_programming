#include "main.h"

/**
 * print_chessboard - prints chess board
 * @a: two dimension array to print
 */

void print_chessboard(char (*a)[8])
{
	int i, j;

	for (i = 0; i < 8; i++)
	{
		for (j = 0; j < 8; j++)
		{
			_putcahr('\n');
		}
		_putchar('\n\);
	}
}
